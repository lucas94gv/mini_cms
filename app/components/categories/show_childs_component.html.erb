<div data-categories-target="childs" class="d-n">
    <% childs.each do |category| %>
        <div>
            <%= category.name %>
            <%= link_to "Editar", edit_category_path(category) %>
            <%= link_to "Eliminar", category, :method => :delete, :data => { :confirm => 'Estás Seguro?' } %>
        </div>
    <% end %>
</div>