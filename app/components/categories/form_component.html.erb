<%= form_with model: @model do |f| %>        
    Nombre: <%= f.text_field :name, required: true %>
    <br>
    Posición: <%= f.number_field :position %>
    <br>
    
    <%= f.hidden_field :ancestry, value: @ancestry unless @ancestry.nil? %>

    <%= f.submit "Guardar" %>
<% end %>